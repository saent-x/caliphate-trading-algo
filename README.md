# Caliphate [no longer maintained]
A trading algorithm implemented using golang to trade crypto and futures markets.

## To Run
To execute the algorithm run the following in the directory containing main.go:

```bash
  go run .
```
